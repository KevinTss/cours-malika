// 1. Demande une valeur à l’utilisateur, calcule le double de cette valeur et affiche le résultat.

// Demander la valeur
var nombre = prompt('Entrez une valeur');

// Calculer le double
var double = Number(nombre) * 2;

alert('Le double de ' + nombre + ' est : ' + double);
